## 개인 다이어리 웹 어플리케이션 요구 사항

### 1. 프로젝트 개요
- **목적**: 사용자가 자신의 일상을 날짜별로 기록하고, 날짜에 따라 자동으로 스타일이 변하는 개인용 다이어리 웹 어플리케이션을 제공한다.
- **대상 사용자**: 일상, 감정, 메모 등을 간단히 기록하고 싶은 개인 사용자.
- **플랫폼**: 웹 브라우저(데스크톱 및 모바일 대응을 목표로 함).

### 2. 주요 기능 요구 사항

#### 2.1 날짜 선택
- **캘린더 또는 날짜 선택 UI**를 통해 사용자가 특정 날짜를 선택할 수 있어야 한다.
- 날짜 형식은 내부적으로 **`yyyyMMdd` 문자열**로 관리한다.  
  - 예: 2026년 5월 6일 → `20260506`
- 날짜를 변경하면 해당 날짜에 저장된 다이어리 데이터를 불러오거나, 데이터가 없을 경우 새로 작성할 수 있는 상태로 표시한다.

#### 2.2 다이어리 작성
- 사용자는 선택한 날짜에 대해 다음 내용을 입력할 수 있다.
  - **제목(title)**: 짧은 텍스트(예: 최대 100자 내외).
  - **본문 내용(content)**: 여러 줄 텍스트 입력 가능.
- 필수 입력 값:
  - 제목과 본문 중 최소 한 개 이상은 입력되어야 저장이 가능하도록 한다(구체 정책은 추후 확정 가능).
- **저장 버튼**을 제공하고, 저장 성공/실패 여부를 사용자에게 알리는 간단한 피드백(알림, 토스트 등)을 제공한다.

#### 2.3 다이어리 조회 및 수정
- 특정 날짜에 이미 저장된 데이터가 있을 경우:
  - 화면 진입 시 자동으로 해당 날짜의 **제목과 본문을 불러와 입력 필드에 표시**한다.
  - 사용자는 내용을 수정한 뒤 다시 저장할 수 있어야 한다(기존 데이터 덮어쓰기).
- 데이터가 없을 경우:
  - 빈 입력 필드를 보여주고 새로 작성할 수 있게 한다.

### 3. 스타일 및 UI 요구 사항

#### 3.1 날짜 기반 배경/텍스트 컬러 자동 생성
- 각 날짜의 **`yyyyMMdd` 값**을 기반으로 다음을 자동 생성한다.
  - **배경 컬러(background color)**
  - **텍스트 컬러(text color)**
- 배경 컬러와 텍스트 컬러는 **가독성이 확보되도록 서로 어울리는 조합**이어야 한다.
  - 예: 배경이 어두운 색이면 텍스트는 밝은 색, 배경이 밝은 색이면 텍스트는 어두운 색.
- 동일한 날짜(`yyyyMMdd`)에 대해 항상 **동일한 색 조합**이 나오도록 결정적(deterministic) 알고리즘을 사용한다.
- 색상은 다이어리 본문 영역에 최소한으로 적용하며, 필요 시 전체 페이지 또는 헤더/푸터에도 동일 컨셉으로 확장할 수 있다.

#### 3.2 반응형 디자인(선택 사항)
- 모바일/데스크톱 환경 모두에서 사용 가능하도록 기본적인 반응형 레이아웃을 적용하는 것을 목표로 한다.
- 작은 화면에서는 입력 필드가 세로로 길게 배치되고, 여백을 줄여 가독성을 높인다.

### 4. 데이터 저장 및 형식 요구 사항

#### 4.1 저장 형식: TSV 파일
- 다이어리 데이터는 **TSV(Tab-Separated Values)** 형식의 파일로 저장한다.
- 기본 컬럼 구조(예시):
  - `date` (`yyyyMMdd`)
  - `title`
  - `content`
- 한 줄당 한 개의 날짜에 대한 기록을 나타내며, 각 필드는 탭 문자(`\t`)로 구분한다.

#### 4.2 TSV 파일 관리 규칙
- 동일한 날짜(`date`)에 대한 데이터가 이미 존재할 경우:
  - 새로운 저장 시 기존 라인을 **갱신(덮어쓰기)** 하는 방식으로 관리한다.
- TSV 파일의 **문자 인코딩**은 UTF-8을 사용한다.
- 줄 구분자는 운영체제 기본 줄바꿈(\n 또는 \r\n)을 사용하되, 프로젝트 내에서 일관되게 유지한다.
- `content` 필드 내 줄바꿈, 탭 문자 등 특수 문자가 TSV 구조를 깨지 않도록 인코딩/이스케이프 전략을 수립한다(예: 줄바꿈을 `\n` 문자열로 치환 등, 구체 방식은 설계 단계에서 정의).

### 5. 비기능 요구 사항

#### 5.1 성능
- 일반적인 개인 사용 시나리오(수백 ~ 수천 건 수준의 기록)에서 저장/조회 시 눈에 띄는 지연 없이 동작해야 한다.

#### 5.2 사용성
- 처음 사용하는 사용자도 직관적으로 이해할 수 있는 단순한 UI를 제공한다.
- 날짜 선택 → 내용 작성 → 저장까지의 흐름이 3~4단계 이내로 자연스럽게 이어져야 한다.

#### 5.3 확장성(선택 사항)
- 향후 다음 기능이 추가될 수 있음을 고려해 설계한다.
  - 특정 기간(예: 한 달) 다이어리 목록 조회
  - 감정 태그, 사진 첨부 등 메타데이터 확장
  - TSV 외 다른 저장 방식(DB, 클라우드 스토리지 등)으로의 마이그레이션

### 6. 오류 처리 및 예외 상황
- TSV 파일 읽기/쓰기 실패 시 사용자에게 오류 메시지를 표시하고, 가능한 경우 재시도 옵션을 제공한다.
- 필수 항목 누락, 유효하지 않은 날짜 등 입력 데이터 검증 실패 시:
  - 어떤 값이 문제인지 명확히 알려주는 검증 메시지를 표시한다.

### 7. 향후 결정이 필요한 사항
- 구체적인 컬러 생성 알고리즘(예: 해시 기반 HSL 변환 등).
- TSV 파일의 실제 파일 경로 및 파일명 규칙.
- 다중 디바이스/브라우저 간 데이터 동기화 여부.
